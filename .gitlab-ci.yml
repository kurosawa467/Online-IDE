image: maven:3.3.9-jdk-8

stages:
    - build
    - test
    - package

build:
    stage: build
    script: mvn clean compile

test:
    stage: test
    script: mvn test

package:
    stage: package
    script: mvn package
    variables:
        MAVEN_OPTS: "-DskipTests=true"
